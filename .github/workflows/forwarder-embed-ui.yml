name: Forwarder Embed UI CI

on:
  push:
    branches: [master]
    paths:
      - "apps/forwarder-ui/**"
      - "apps/shared-ui/**"
      - "services/forwarder/**"
      - "package-lock.json"
      - "package.json"
      - ".github/workflows/forwarder-embed-ui.yml"
  pull_request:
    branches: [master]
    paths:
      - "apps/forwarder-ui/**"
      - "apps/shared-ui/**"
      - "services/forwarder/**"
      - "package-lock.json"
      - "package.json"
      - ".github/workflows/forwarder-embed-ui.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  forwarder-embed-ui:
    name: Forwarder embed-ui build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust artifacts
        uses: Swatinem/rust-cache@v2

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install npm dependencies
        run: npm ci

      - name: Install Rollup Linux optional dependency workaround
        if: runner.os == 'Linux'
        run: npm install --no-save @rollup/rollup-linux-x64-gnu

      - name: Build and test forwarder with embedded UI
        run: cargo test -p forwarder --features embed-ui --test ui_embed_fallback
